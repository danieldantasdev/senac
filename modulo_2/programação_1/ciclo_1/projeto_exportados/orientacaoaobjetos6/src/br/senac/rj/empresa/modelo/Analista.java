package br.senac.rj.empresa.modelo;

import br.senac.rj.empresa.modelo.Funcionario;

public class Analista extends Funcionario {
    // não precisamos fazer nada aqui, porque o método já está implementado, vai ser
    // herdado da classe mãe (Funcionario)
    // Podemos criar métodos específicos para a classe filha, mas não é necessário
    // Podemos instanciar objetos na classe Analista, herdando os atributos e
    // métodos públicos ou protected da classe mãe

}
